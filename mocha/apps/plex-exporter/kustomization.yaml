---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Kept described here even though the Application below sets CreateNamespace:
  # dropping it makes ArgoCD list the namespace as awaiting pruning, and pruning a
  # namespace takes everything in it.
  - namespace.yaml
  - app.yaml
