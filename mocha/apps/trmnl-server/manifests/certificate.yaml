---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: trmnl-server-tls
spec:
  secretName: trmnl-server-tls
  dnsNames:
    - trmnl.mocha.thoughtless.eu
  issuerRef:
    name: cloudflare
    kind: ClusterIssuer
