apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: pr-gitops-grafana-annotation
  namespace: argocd
spec:
  project: default
  syncPolicy:
    automated:
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
  destination:
    server: https://kubernetes.default.svc
    namespace: pr-gitops-grafana-annotation
  source:
    repoURL: https://github.com/qjoly/gitops.git
    targetRevision: HEAD
    path: mocha/apps/pr-gitops-grafana-annotation/manifests
