apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: abcdesktop
  namespace: argocd
spec:
  project: default
  syncPolicy:
    automated:
      selfHeal: true
      # od.config is a hashed ConfigMap: without prune the superseded ones pile
      # up and keep the app OutOfSync forever.
      prune: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
  destination:
    server: https://kubernetes.default.svc
    namespace: abcdesktop
  source:
    repoURL: https://github.com/qjoly/gitops.git
    targetRevision: HEAD
    path: mocha/apps/abcdesktop/manifests
