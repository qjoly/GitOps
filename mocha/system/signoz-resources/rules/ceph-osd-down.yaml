apiVersion: resources.signoz.io/v1alpha1
kind: Rule
metadata:
  name: ceph-osd-down
  namespace: signoz
  annotations:
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  providerConfigRef:
    name: mocha
  objectTemplate:
    spec:
      schemaVersion: v2alpha1
      alert: Ceph OSD down
      alertType: METRIC_BASED_ALERT
      ruleType: threshold_rule
      description: At least one ceph_osd_up = 0 on turing.
      labels:
        severity: critical
      annotations:
        summary: A Ceph OSD is down
        description: At least one ceph_osd_up = 0 on turing.
      condition:
        compositeQuery:
          queryType: builder
          panelType: graph
          queries:
          - type: builder_query
            spec:
              name: A
              signal: metrics
              disabled: false
              aggregations:
              - metricName: ceph_osd_up
                timeAggregation: latest
                spaceAggregation: min
              filter:
                expression: k8s.cluster.name = 'turing'
              groupBy:
              - name: k8s.cluster.name
        selectedQueryName: A
        thresholds:
          kind: basic
          spec:
          - name: critical
            op: below
            matchType: at_least_once
            target: 1
            channels:
            - discord
      evaluation:
        kind: rolling
        spec:
          evalWindow: 5m
          frequency: 1m
      notificationSettings:
        usePolicy: false
        renotify:
          enabled: false
