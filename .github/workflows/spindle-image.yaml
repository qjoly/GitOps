name: spindle-image
# Upstream publishes no spindle image (only atcr.io/tangled.org/knot), so we
# build it ourselves from the monorepo's localinfra/spindle.Dockerfile.
on:
  workflow_dispatch:
    inputs:
      ref:
        description: tangled.org/core tag to build
        default: v1.16.1-alpha
        required: true
permissions:
  contents: read
  packages: write
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Clone tangled.org/core
        run: git clone --depth 1 --branch "${{ inputs.ref }}" https://tangled.org/tangled.org/core core

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/build-push-action@v6
        with:
          context: core
          file: core/localinfra/spindle.Dockerfile
          platforms: linux/amd64
          push: true
          tags: ghcr.io/qjoly/spindle:${{ inputs.ref }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
