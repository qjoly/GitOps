version: '3'
tasks:
  debian:
    desc: "Run Debian-Packer"
    deps: [ general:env ]
    dotenv: ['.env', '{{.ENV}}/.env.', '{{.HOME}}/.env']
    cmds:
      - echo "Here run Packer"
    preconditions:
      - if [ "$vmtemplate_debian_enabled" = "true" ]; then exit 0; else exit 1; fi
      - sh: '[ $vmtemplate_debian_enabled = "true" ]'
