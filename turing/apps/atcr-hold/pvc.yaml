# Embedded PDS: SQLite carstore + signing key. ceph-block, never NFS.
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: atcr-hold-data
  namespace: atcr-hold
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ceph-block
  resources:
    requests:
      storage: 5Gi
