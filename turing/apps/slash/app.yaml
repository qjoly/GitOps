apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: slash
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://rubxkube.github.io/common-charts/
    chart: common
    targetRevision: v0.6.0
    helm:
      values: |
        name: "slash"
        image:
          repository: yourselfhosted/slash
          tag: "1.0.0-rc.2"
        service:
          servicePort: 5231
        deployment:
          port: 5231
        ingress:
            enabled: true
            hostName: "slash.thoughtless.eu"
            ingressClassName: traefik
            tls:
              enabled: true
              secretName: "slash-tls"
            annotations:
              cert-manager.io/cluster-issuer: cloudflare
              external-dns.alpha.kubernetes.io/cloudflare-proxied: "true"
              external-dns.alpha.kubernetes.io/hostname: "slash.thoughtless.eu"
              external-dns.alpha.kubernetes.io/target: "58c15097-f3b2-4ff7-a676-21bbeeea39d7.cfargotunnel.com"
        variables:
          secret: {}
          nonSecret:
            SLASH_PORT: "5231"
        persistence:
          enabled: true
          volumes:
            - name: data
              storageClassName: ""
              size: 5Gi
              pvcClaim: ""
              containerMount: "/var/opt/slash"
  destination:
    server: https://kubernetes.default.svc
    namespace: slash
  syncPolicy:
    automated:
      selfHeal: true
      prune: true
    syncOptions:
      - CreateNamespace=true
