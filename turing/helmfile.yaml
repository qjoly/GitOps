# Applied in CI with helm 3.x (helmfile v0.171.0 is incompatible with helm 4).
repositories:
  - name: cilium
    url: https://helm.cilium.io/

releases:
  - name: cilium
    namespace: kube-system
    chart: cilium/cilium
    version: "1.19.6"
    values:
      - helm/cilium/values.yaml
